node_modules/
dist/
*.tsbuildinfo
coverage/

# Swiss Ephemeris data files: downloaded by script, not versioned.
# See packages/core/scripts/download-ephe.mjs
ephe/
*.se1

# Imported GeoNames dataset: a regenerable artefact of ~90 MB.
# See packages/geo/scripts/import-geonames.mjs
packages/geo/data/

.env
.env.*
!.env.example

.DS_Store
.svelte-kit/
build/

compose.override.yaml

# Scans of the primary and secondary sources, and scratch work against them.
# Most were bought and cannot be redistributed, and the largest is ~96 MB.
# What a source establishes belongs in docs/sources.md, which cites by title
# and never by path: nothing in the repository depends on either directory
# existing. See texts/README.md.
texts/
temp/
